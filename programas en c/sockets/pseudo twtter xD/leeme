//compilamos
$ gcc server.c -o server
$ gcc client.c -o client

//al ejecutar el server se asigna un puerto para escuchar
./server 80

//al ejecutar el cliente se le da la ip del servidor y el puerto por el que estaba escuchando

./client 192.168.1.58 80
